# v0.6.0 Build Sheet (grounded + adversarially reviewed)

Source of truth for v0.6.0 implementation. 9 agents (4 ground + 4 review + synth),
2026-06-01. Implement main-loop, compile-green each step. North star: transparent
local, free-first/BYOK search and extraction routing with no synthesis. Scope =
D1 trace opt-in, D2 search_and_extract, D3 research→core + drop synthesizeSummary.
(honest-quota deferred.)

## Build sequence
1. **core/types.go** — add `SearchAndExtractRequest{Query, Task omitempty, Limit, ExtractTop}`,
   `SearchAndExtractResponse{Search SearchResponse, Extracts []ExtractResponse, ExtractErrors []ExtractError omitempty}`,
   `ExtractError{URL, Error}`. NO IncludeTrace in core (trace strip is a surface concern).
2. **core/research.go (NEW)** — move `ResearchReport{Question,Sources,Extracts,Providers,Steps}` (NO Summary),
   ResearchSource, ResearchExtract, `func (s *Service) Research(ctx,question,maxSteps)(*ResearchReport,error)`
   (= cli researchPipeline body MINUS synthesizeSummary call+assignment), `classifiedResearchTasks` (bare core prefixes).
   Keep both ctx.Err() guards, 2000-rune cap, .pdf/reddit skip, min(len,5). Imports: context,fmt,os,sort,strings,unicode/utf8,safeerr.
   No import cycle (core ⊥ cli/mcpserver; safeerr→providerhttp acyclic). Keep os.Stderr step-fail logs verbatim (document in CHANGELOG).
3. **core/service.go** — `const maxExtractTop = 3`; `func (s *Service) SearchAndExtract(ctx, req) (resp, error)`:
   clamp n=ExtractTop (<=0→1, >3→3); `searchResp,err=s.Search(...)` err→fatal return; out{Search:searchResp};
   seen-set dedup; range Results, skip empty/seen URL; `s.Extract(...)`; on eerr: `if ctx.Err()!=nil return out,ctx.Err()` FIRST
   else append ExtractError{URL, safeerr.Message(eerr)}; else append Extracts; attempted++; break at n. SEQUENTIAL. Import safeerr. NEVER eerr.Error().
4. **core tests** — NEW core/research_test.go (MOVE cancellation + fan-out tests from cli), core/service_searchextract_test.go
   (perURLExtractProvider{fakeProvider; failURL; calls[]} + multi-URL literal-IP search fake: 93.184.216.34 good, 169.254.169.254 blocked;
   countingLedger). DELETE moved tests from cli/research_test.go:18-37 + cli/v050_test.go:74-99. no-summary = JSON-key-presence guard.
5. **mcpserver/tools.go** — D1: `mcp.WithBoolean("include_trace", Desc...)` on search+extract; in handlers AFTER err-return,
   `if !req.GetBool("include_trace",false){ resp.RouteTrace=nil }` (success path only; error path keeps trace — error_test.go:17).
   D2: searchAndExtractToolDescription const; register `search_and_extract` INSIDE HasExtractCapableConfigured() block
   (query req, task enum, limit, extract_top, include_trace); handler → svc.SearchAndExtract; on !includeTrace strip resp.Search.RouteTrace
   AND loop resp.Extracts[i].RouteTrace; error via toolErrorJSON(...,resp.Search.Route,resp.Search.RouteTrace). No setup_tip.
   D3: register `research` UNCONDITIONALLY (question req, max_steps); handler → svc.Research; error = `mcp.NewToolResultError(safeerr.Message(err))`
   (NOT toolErrorJSON — ResearchReport has no route/trace). Import safeerr.
6. **cli/http.go** — D1: `IncludeTrace bool json:"include_trace"` on /api/search,/api/extract structs; strip after err-return before Encode.
   D2: POST /api/search_and_extract (405 non-POST, MaxBytesReader 1<<20, decode, NormalizeTaskParam, svc.SearchAndExtract,
   strip both traces if !IncludeTrace, err→buildCLIError(...,resp.Search.Route,resp.Search.RouteTrace)). D3: POST /api/research
   (decode {question,max_steps}, default 3, svc.Research, err→writeHTTPJSONError(500,{operation:"research",error:safeerr.Message})).
7. **cli/research.go** — DELETE type decls + researchPipeline + classifiedResearchTasks + synthesizeSummary; RunE → `svc.Research`;
   rewrite printResearchReport(*core.ResearchReport): header + sources + extracts (print-only TruncateRunes ~200 preview + "full in --json"), NO summary line.
   Update cobra Short/Long (drop "synthesis with citations"/"Synthesizes a cited summary"; KEEP the 2 cost-policy qualifier strings).
   Trim imports to: encoding/json, fmt, strings, core, cobra (drop os/sort/unicode/utf8/safeerr).
8. **surface tests + CHANGELOG + README** — D1/D2/D3 MCP+REST tests (trace default-off both polarities; tools/list presence:
   search_and_extract present w/ TAVILY absent w/ only BRAVE, research unconditional; no-summary key guard; extend TestRESTWriteEndpointsRejectNonPOST
   with the 2 new routes + malformed/oversized 400). CLI help guard (no "Synthesizes a cited summary"). CHANGELOG [0.6.0] (2 semi-breaking notes:
   trace opt-in, research no-summary; + server-stderr note). README pointer.
9. **gate + diff review + PR** — gofmt/vet/go test -race/audit/secret-scan; self-review strip placement; adversarial diff-review workflow; PR + Codex.

## Resolved decisions
- extract_top: default 1, clamp [1,3] (maxExtractTop=3), before loop.
- Extracts SEQUENTIAL (n<=3, WriteTimeout 300s; concurrency deferred).
- Trace strip: at MCP/REST boundary only (NOT in core req); strip Search.RouteTrace + every Extracts[i].RouteTrace; routing_insight survives.
- research error envelope: distinct sanitized (safeerr.Message), NOT toolErrorJSON/buildCLIError (no route/trace on ResearchReport).
- search_and_extract GATED on HasExtractCapableConfigured(); research UNCONDITIONAL (returns sources even w/o extract = honest evidence).
- MOVE (not dup) cancellation + fan-out tests into core; delete from cli (cli won't compile referencing removed symbols — atomic).
- Drop ONLY Summary field + synthesizeSummary; guard with JSON-key-presence (not substring).
- Keep os.Stderr step-fail logging verbatim; document the server-stderr side effect.
- Truncate ONLY in CLI printer (~200); core/wire/JSON keep FULL content (no router quality judgment).

## Residual risks
- 2 semi-breaking: trace omitted-by-default (MCP/REST), research summary gone everywhere → CHANGELOG must call out.
- core.Research logs to os.Stderr on degraded substeps (server log interleave) — accepted, documented.
- New core tests use literal-IP URLs (net-free); don't add example.com hermetic tests.
- search_and_extract can return up to 3 FULL extracts (large) — honest, document; extract_top=1 for smaller.
- D1 strip MUST be after err-return; search_and_extract strips BOTH search + per-extract traces (most error-prone spot).
- GetBool advisory (malformed → false → trace omitted, safe direction).
